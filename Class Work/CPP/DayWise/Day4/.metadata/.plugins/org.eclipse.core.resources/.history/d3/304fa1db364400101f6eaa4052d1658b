#include<iostream>
#include "../MyHeaderFiles/Calculator.h"
#include "../MyHeaderFiles/Calculator.h"
using namespace std;


int main()
{
	int a=100, b=200;

	printf("The Sum is: %d", Add(a,b));
	printf("\nThe Sub is: %d", Sub(a,b));
	printf("\nThe Mul is: %d", Mul(a,b));
	printf("\nThe Div is: %d", Div(a,b));

	return 0;
}
